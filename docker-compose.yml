version: '3.8'

services:
  bot:
    build: .
    ports:
      - "10000:10000"
    environment:
      - TELEGRAM_TOKEN=${TELEGRAM_TOKEN}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    restart: unless-stopped